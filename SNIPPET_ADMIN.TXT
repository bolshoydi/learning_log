learning_log admins:
в этом проекте в сsettinhs .py подключена твоя постгрею на момент написания это было 9.6.
на порту 5434
база данных на пг:learning_log_db
валделец и пользователь: dimas (переделал на postgres)
пасворд: admin

также создан суперюзер ПРОЕКТА learning_log через manage.py createsuperuser
user: ll_admin
pass: ll_app#admin
этот юзер пароль будешь использовать в админке localhost:8000/admin

чтобы достать записи по внешнему ключу foreign_key нужно запросить у модели так
t = связанная_модель.objects.get(id = 1)
t.имя_внешней связи_set.all()
например:
from django.db import models


class Topic(models.Model):
    """Тема, которую изучает пользователь"""
    text = models.CharField(max_length=200)
    date_added = models.DateTimeField(auto_now_add=True)

    def __str__(self):
        """строковое представление"""
        return self.text


class Entry(models.Model):
    """информация изученная пользователем по теме"""
    topic = models.ForeignKey(Topic)
    text = models.TextField()
    date_added = models.DateTimeField(auto_now_add=True)

    
тогда обращение будет таким:
>>> from learning_logs.models import Topic
>>> t = Topic.objects.get(id=1)                                               
>>> t.text                                                                    
'Chess'                                                                       
>>> t.date_added                                                              
datetime.datetime(2017, 7, 9, 0, 0, tzinfo=<UTC>)                             
>>> t.entry_set.all()                                                         
<QuerySet [<Entry: Ша́хматы — настольная логическая игра со специальн...>]>   
>>>                                                                           
                                                                              